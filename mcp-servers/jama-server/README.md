# Jama MCP Server (Placeholder)

This server is not yet implemented. Jama API authentication is currently blocked.

Once authentication is resolved, this server will provide read-only access to Jama requirements via MCP tools, replacing the Excel fallback provider.

See `providers/jama/fetch.md` for the planned API surface.
